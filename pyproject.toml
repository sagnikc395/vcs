[project]
name = "vcs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.4.2",
    "configparser>=7.2.0",
    "pytest>=9.1.1",
    "rich>=15.0.0",
]
